# “Class name” meant the attribute Template.render

They heard “the class name is probed_render” as the class being renamed. Floor: the class is still `Template`. `Template.render` is an attribute that holds a function object; after assignment that attribute holds `probed_render`. Say **class attribute**, not “class name.”

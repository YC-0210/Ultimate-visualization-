Altitude: structural

# Request felt like a black box

They asked to see what is inside `request` on lesson 0005. Floor: they already dump fields off it; they did not have a picture of one object with pockets — some arrived (method, path, COOKIES), some stamped later (session, user, resolver_match). COOKIES (dict) vs Cookie header (string) is the type split to keep using.

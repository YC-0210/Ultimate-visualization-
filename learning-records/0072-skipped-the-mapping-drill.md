Altitude: mechanical

# They declined the kind-mapping drill

Asked to list the kinds their real GET produces, they said they know the concept and want to move on. Do not re-run that retrieval exercise. Preference to honour: when they say the concept is held, go straight to the next mechanic and put the check on the *code output* instead of on a restatement. The floor from LR-0070 is still unverified, so watch for `kind` / `type` conflation in the `stages` list they produce rather than asking about it again.

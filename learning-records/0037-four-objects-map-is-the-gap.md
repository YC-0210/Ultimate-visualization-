Altitude: structural

# Gap: four objects in the slug lookup

They named a weak map — queryset vs model vs serializer vs instance — and said that is why SlugRelatedField does not click. Lesson 0011 fills that map on `POST /api/cart/item/` before returning to the field.

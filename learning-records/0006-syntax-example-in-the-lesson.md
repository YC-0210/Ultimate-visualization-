Altitude: mechanical

# Syntax for the four-field dump belongs in the lesson

The peel was clear; they did not know how to type `getattr`, the `view_class` branch, or `json.dumps` of four keys. They asked for a copyable example in the lesson, not in `middleware.py`. Lesson 0002 now has that block. Wiring it after `get_response` (and not dumping the response object) is still theirs. Later lessons should include a shape-of-the-code block when the practice is “type this.”

# Request is the object layers stamp; that is what P1 reads

They stated it: the request accumulates information, and that is what we want to see. Floor. Two hedges for later: some pockets *arrived* with the diner (path, Cookie), they were not stamped; not every middleware writes on the request (some write on the response on the way out). The response is still the other object — P1.3 just does not dump it yet.

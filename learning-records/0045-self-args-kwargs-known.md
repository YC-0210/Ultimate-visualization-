Altitude: mechanical

# They can read self, *args, **kwargs

They finished the docs check: `concat("earth", "mars", "venus")` fills `args` with the three names and `sep` with `"/"`. They stated they now know what `self`, `*args`, and `**kwargs` do. Floor: those names are legal on the wrap; next is the P1.7 print.

# FastAPI was an undefined second framework

They asked what FastAPI is. Depth now: one line from its docs — another Python web framework, not Django, not the restaurant app. In this repo it is only the Phase 6 example of a second adapter. Do not use that name (or Express) as an unexplained foil; gloss it or say “a second adapter.”

# They asked for self / *args / **kwargs

The remaining block on writing the wrap from scratch is Python’s function slots, not DRF types. Lesson 0012 maps `self`, `*args`, and `**kwargs` on their `probed_is_valid` line.

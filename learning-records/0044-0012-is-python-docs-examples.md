Altitude: mechanical

# 0012 is Python slots, docs examples first

They asked for a separate Python explanation of `self`, `*args`, `**kwargs` using the tutorial examples, not the serializer wrap. Lesson 0012 now uses `MyClass.f`, `concat`, and `cheeseshop` from the Python docs.

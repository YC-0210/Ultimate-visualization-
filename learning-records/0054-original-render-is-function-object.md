Altitude: mechanical

# original_render is a function object

They named the thing `original_render` holds as a function object — the 0014 type. Floor: it is Django’s original `Template.render`, still that same object after the class name is pointed at `probed_render`. An instance lookup `t.render` is a method object, not that function object.

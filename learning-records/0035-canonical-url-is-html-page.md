# Canonical capture URL is the HTML menuitem page

They chose `GET /menuitem/經典原味鍋/` (`menuitem_page`, FBV) as the lesson practice URL. The JSON API is only for tasks that need a class view or serializer. Copyable code in a lesson is two blocks: a clean snippet, then the same snippet with a comment on each line.

Altitude: mechanical

# P1.4 view facts captured

They captured JSON-safe class names for `permission_classes`, `serializer_class`, and `queryset.model` from the resolved view class. On `GET /api/menuitem/經典原味鍋/`, the output correctly showed `AllowAny`, `menuitemSerializer`, and the `menuitem` model.

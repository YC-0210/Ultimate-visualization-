Altitude: structural

# P1.9 ends at a file; sending is Phase 3

They summarised P1.9 as "send the captured information in the form of a trace." The reshape half is right; "send" is not. P1.9 writes one file per request and stops — the panel loading it is P2.5, the SSE push is P3.2. Also corrected: a Trace is a shape, not a transport, so a file with nothing sent is already a Trace. Watch for the same conflation when P3.2 arrives.
